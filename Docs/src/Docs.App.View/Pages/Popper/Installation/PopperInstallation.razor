@namespace Skclusive.Material.Docs.App.View

<MarkdownSegment
    Text="@(@"
# Popper Installation

if you want to use Popper alone follow the standalone installation. otherwise to use all components, [compound installation](./installation) can be followed.

You can [install Popper package](https://www.nuget.org/packages/Skclusive.Material.Popper/) by typing the below command in your terminal:

```csharp
dotnet add package Skclusive.Material.Popper
```

Then, you can import it in your `_Imports.razor`.

```csharp
@using Skclusive.Material.Popper
```

Then in your `App.razor`, add `ThemeProvider` wrapping Router.

```html
<ThemeProvider>
    <Router AppAssembly=""@typeof(Program).Assembly"">
        ...
    </Router>
</ThemeProvider>
```

Then in your `services (ServiceCollection)` add below service.

```csharp
services.TryAddPopperServices(new MaterialConfigBuilder().Build());
```

")" />
